<h1>Welcome <%= current_user.name %></h1>

<h3>It's <%= Time.now.strftime('%F') %></h3>
<h3>We have <%= pluralize(@total_orders, 'order') %>.</h3>

<div class="container-fluid admin-index">
  <%= link_to "Items", admin_items_path, class:"admin-button col-xs-4 col-sm-2" %>
  <%= link_to "Orders", orders_path, class:"admin-button col-xs-4 col-sm-2" %>
  <%= link_to "Admin Logout", logout_path, method: :delete, class:"admin-button col-xs-4 col-sm-2", data: { confirm: 'Are you sure?' } %>
</div>
