<div class="page-header">
  <%= link_to cart_path(@current_cart), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <h1>New order</h1>
</div>

<%= render 'form' %>
